spring:
  application:
    name: see-something-abq

  profiles:
    active:
      - service
      - local

  datasource:
    url: "jdbc:h2:file:./db/see-something-abq"

  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true

  sql:
    init:
      mode: always
